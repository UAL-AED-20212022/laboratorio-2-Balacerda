import view as v
import sys

if __name__ == "__main__":
    sys.stdout.reconfigure(encoding="utf-8") 
    v.main()

#  Neste programa, procuramos utilizar listas ligadas (LinkedList.py), disponibilizado pela profressora,
# e chamar as funçoes disponiveís no mesmo. Os requisitos do programa é a utlização do ficheiro LinkedList.py,

# O resultado esperado deste programa, é, primeiramente, o bom funcionamento do mesmo, e ....